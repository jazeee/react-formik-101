import { IBasicFormFields } from './types';

interface Props {
  initialValues: IBasicFormFields | undefined;
  setValues(values: IBasicFormFields): void;
}

export const BasicForm = (props: Props) => {
  // const { initialValues, setValues } = props;
  return null;
};
